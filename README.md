# FindCat

A small Python script that scrapes the [Cat Wikipedia page](https://en.wikipedia.org/wiki/Cat), finds `<img>` tags whose URL contains "cat", and downloads them into a local `cat_images/` folder.

## Tech stack

Python, `requests`, `beautifulsoup4`

## Usage

```
pip install requests beautifulsoup4
python main.py
```
